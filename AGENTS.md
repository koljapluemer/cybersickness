# Project Instructions

Check lint and build after finishing a feature.

Do not run `npm run dev`, `vite`, or any command that starts the frontend dev server.

Do leave "fallbacks" to confirmed broken code.
Do not leave old, dead code for "legacy" reasons.
Implement one solid, pattern-driven architecture, not 17 layers of leaking "fallback" garbage.